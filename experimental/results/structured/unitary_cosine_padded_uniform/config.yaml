general:
  name: "structured_unitary_cosine"
  recon: "spec"
  save: True

signal:
  shape: [1,1,64,64]
  mode: ["unit","shepp-logan"]
  config:

model:
  explicit_spectrum: "unit"
  n_layers: 2
  diagonal:
    #! left one is closer to the input
    mode: [['unit','uniform'],['unit','uniform']] # mag: 'unit', 'marchenko', phase: 'uniform', 'laplace', 'quadrant'
    config:
      #degree_of_freedom: 30
      #radius: 0.8
  transform: "cosine2" # 'fourier', 'cosine', 'hadamard' + dimension
  pad_powers_of_two: True
  shared_weights: False

recon:
  series: "arange"
  start: 2
  end: 194
  list: []
  n_repeats: 50
  max_iter: 5000

save:
  path: "../runs"
  name: "res_{model_name}_{structure}_{img_mode[0]}_{img_mode[1]}_{recon}.csv"