general:
  name: "marchenko_hadamard2"
  recon: "spec"
  save: True
  verbose: False

signal:
  shape: [1,1,64,64]
  mode: ["unit","random"] #["delta","constant"]
  config:
    n_layers: 0

model:
  spectrum:
    mode: "unit"
    product: 1
  n_layers: 2
  diagonals:
    #! in the order of math
    mode: [['unit','uniform'],['marchenko','uniform']] # mag: 'unit', 'marchenko', phase: 'uniform', 'laplace', 'quadrant'
    config:
      #degree_of_freedom: 30
      #radius: 0.8
  transforms: ["hadamard2","hadamard2"] # 'fourier', 'cosine', 'hadamard' + dimension
  pad_powers_of_two: False
  shared_weights: False
  include_zero: True

recon:
  series: "arange"
  start: 2
  end: 194
  list: []
  n_repeats: 50
  max_iter: 5000

save:
  path: "../runs"
  name: "{model_name}_{structure}_{recon}.csv"
